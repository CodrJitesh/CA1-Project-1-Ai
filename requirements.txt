# Core agent + rule lane + notebook: standard library only.
# The following are needed only for the LLM lane (mode="llm") and to
# execute the notebook headless.
openai>=2.0.0
python-dotenv>=1.0.1
jupyter>=1.0
nbconvert>=7.0
